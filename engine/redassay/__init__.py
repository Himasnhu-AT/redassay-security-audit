"""redassay - deterministic security scanning with a human triage loop.

The package is intentionally dependency-free: everything here runs on a stock
CPython 3.9+ install so the plugin works without a pip step.
"""

__version__ = "0.1.0"
