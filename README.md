# redassay

Security audit loop for Claude Code.

`/redassay-security` walks a codebase, finds vulnerabilities, records them in a
JSON findings repo, and serves a local review board where a human decides what
actually gets fixed.

Status: early. See `docs/` for design notes.
